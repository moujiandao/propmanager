# Agent Instructions

The canonical project guide for AI coding agents lives in [`CLAUDE.md`](./CLAUDE.md) — architecture, database tables, conventions, non-obvious decisions, common tasks, and project-specific anti-patterns.

**Read `CLAUDE.md` before making changes.** This file exists so tools that look for `AGENTS.md` (e.g. Codex) find the same instructions; it is intentionally a pointer, not a second copy, so the two can't drift.
